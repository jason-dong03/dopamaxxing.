'use client'
// kept as a no-op export so existing imports don't break — logic moved to DashboardHeader
export default function MobileExpand(_props: {
    loginStreak: number
    activeTitle?: string | null
    adminPanel: boolean
}) {
    return null
}
